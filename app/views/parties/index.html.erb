<p id="notice"><%= notice %></p>

<h1>Listing Parties</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>
      <th>Max attendees</th>
      <th>Age range</th>
      <th>Description</th>
      <th>What to bring</th>
      <th>Rules</th>
      <th>Host</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parties.each do |party| %>
      <tr>
        <td><%= party.name %></td>
        <td><%= party.date %></td>
        <td><%= party.max_attendees %></td>
        <td><%= party.age_range %></td>
        <td><%= party.description %></td>
        <td><%= party.what_to_bring %></td>
        <td><%= party.rules %></td>
        <td><%= party.host %></td>
        <td><%= link_to 'Show', party %></td>
        <td><%= link_to 'Edit', edit_party_path(party) %></td>
        <td><%= link_to 'Destroy', party, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Party', new_party_path %>
